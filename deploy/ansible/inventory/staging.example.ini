[staging]
# Replace ansible_host and ansible_user with the host/user used to bootstrap the server.
# The user must be able to become root through sudo.
pegelhub-staging ansible_host=203.0.113.10 ansible_user=root
