# Copy this file into a connector instance directory as connector.env.
# Connector configuration and credentials belong in the sibling config/
# directory. Do not commit the installed file.

COMPOSE_PROJECT_NAME=pegelhub-connector
PEGELHUB_CONNECTOR_IMAGE=replace-with-connector-image
PEGELHUB_TRUST_MODE=system
CONNECTOR_JAVA_TOOL_OPTIONS=
