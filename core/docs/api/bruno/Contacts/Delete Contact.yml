info:
  name: Delete Contact
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/{{apiPath}}/contact/{{uuid}}"
  headers:
    - name: X-Auth-Profile
      value: operator
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: bru.setVar('uuid', bru.getVar('contactId'));
    - type: after-response
      code: test('status is 200', () => expect(res.getStatus()).to.equal(200));

docs: Deletes a legacy contact by id.
