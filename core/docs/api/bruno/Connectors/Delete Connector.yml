info:
  name: Delete Connector
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/{{apiPath}}/connectors/{{uuid}}"
  headers:
    - name: X-Auth-Profile
      value: operator
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: bru.setVar('uuid', bru.getVar('connectorId'));
    - type: after-response
      code: test('status is 200', () => expect(res.getStatus()).to.equal(200));

docs: Deletes connector metadata by id. Run deliberately; this breaks the write workflow.
