info:
  name: List Observed Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/{{apiPath}}/observed-properties"
  headers:
    - name: X-Auth-Profile
      value: operator
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test('status is 200', () => expect(res.getStatus()).to.equal(200));

docs: Reads the canonical observed-property and source-representation catalog.
