version: "3.8"
services:
  pegelhub-ma-connector:
    build: .
    image: pegelhub-ma-connector
    devices:
      - "/dev/piControl0:/dev/piControl0"
    volumes:
      - ./files:/app/files:ro
    environment:
      JAVA_TOOL_OPTIONS: "-DLOG_LEVEL=INFO"